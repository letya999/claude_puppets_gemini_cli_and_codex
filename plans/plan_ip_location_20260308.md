# Plan: Create IP Location Script
**Goal:** Create a Python script that detects the current public IP and shows the city and country.

## Steps
1. [x] Create `ip_loc.py` using `requests` and `ipapi.co` (or similar free service).
2. [x] Add error handling for network issues.
3. [x] Verify the script by running it and checking the output.

## Technical Details
- Language: Python 3
- Dependencies: `requests`
